{
  "name": "opencode-go-balance-web",
  "version": "1.0.0",
  "description": "Standalone Docker-ready OpenCode Go usage dashboard",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/huiyio/dsh-opencode-go-box.git"
  },
  "bugs": {
    "url": "https://github.com/huiyio/dsh-opencode-go-box/issues"
  },
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "preview": "node scripts/start-local-preview.js",
    "test": "node --test",
    "check": "node --check server.js && node --check scripts/start-local-preview.js && node --check src/config.js && node --check src/key-store.js && node --check src/account-service.js && node --check src/usage-service.js && node --check src/model-test-service.js && node --check src/http-server.js && node --check public/app.js && node --check public/admin.js"
  }
}
